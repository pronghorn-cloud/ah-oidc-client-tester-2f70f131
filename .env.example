# OIDC Testing Client Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# React App Configuration (used during build)
REACT_APP_API_URL=http://localhost:3001

# Optional: Default Identity Provider
# REACT_APP_DEFAULT_ISSUER=https://your-idp.example.com
